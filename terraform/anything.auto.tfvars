name = "there we go!!!"

my_map = {
  key_one = "1"
  key_two = ["number", "2"]
}

# curl -sLO https://github.com/shihanng/tfvar/releases/latest/download/tfvar_linux_amd64.deb
# sudo dpkg -i tfvar_linux_amd64.deb

# tfvar . -e -a
# export TF_VAR_my_map='{ key_one = "1", key_two = ["number", "2"] }'
# export TF_VAR_name='there we go!!!'
